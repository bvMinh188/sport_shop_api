<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar navbar-light bg-light" href="/admin/addProduct">Thêm sản phẩm </a>
  <a class="navbar navbar-light bg-light" href="/admin/addCategory">Thêm loại sản phẩm </a>
  <a class="navbar navbar-light bg-light" href="/admin/users">Thông tin người dùng</a>
  <a class="navbar navbar-light bg-light" href="/admin/transaction">Đơn hàng</a>
</nav>

<div class="mt-4" style="padding: 40px;"; >
    <h3>Thêm sản phẩm</h3>

    <form method="post" action="/admin/added">
        <div class="form-group">
            <label for="name">Tên sản phẩm</label>
            <input type="text" class="form-control" id="name" name="name" required>
        </div>

        <div class="form-group">
            <label for="description">Mô tả</label>
            <textarea type="text" class="form-control" id="description" name="description" required></textarea>
        </div>

        <div class="form-group">
            <label for="image">Hình ảnh</label>
            <input type="text" class="form-control" id="image" name="image" required>
        </div>

        <div class="form-group">
            <label for="price">Giá</label>
            <input type="text" class="form-control" id="price" name="price" required>
        </div>

        <div class="form-group">
            <label for="category">Loại giày</label>
            <select class="form-control" id="category" name="category" required>
                <option value="" disabled selected>Loại giày</option>
                {{#each categories}}
                    <option value="{{this.name}}">{{this.name}}</option>
                {{/each}}
            </select>
        </div>

        <div class="form-group">
            <label>Số lượng</label>
            <div id="size-inputs">
                {{#each sizes}}
                    <div class="form-row align-items-center mb-3">
                        <label class="col-sm-2 col-form-label">Size {{this}}</label>
                        <div class="col-sm-1">
                            <input type="number" class="form-control" name="sizes[{{@index}}][quantity]" placeholder="Số lượng" min="0" value="0" required>
                            <input type="hidden" name="sizes[{{@index}}][size]" value="{{this}}">
                        </div>
                    </div>
                {{/each}}
            </div>
        </div>

        <button type="submit" class="btn btn-primary">Thêm sản phẩm</button>
    </form>
</div>
